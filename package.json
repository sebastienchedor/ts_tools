{
	"name": "api_external_partners",
	"version": "0.0.1",
	"description": "CheckAndVisit External Partner Api",
	"main": "index.js",
	"author": "",
	"license": "MIT",
	"dependencies": {},
	"devDependencies": {
		"@types/chai": "^4.2.7",
		"@types/mocha": "^5.2.7",
		"@types/node": "^13.13.12",
		"chai": "^4.2.0",
		"mocha": "^7.2.0",
		"ts-node": "^8.10.2",
		"typescript": "^3.9.6"
	},
	"scripts": {
		"build": "tsc",
		"clean": "rm -r .build/* && rm package-lock.json && rm -r node_modules",
		"test": "npm run build && mocha --require ts-node/register test/index.ts --exit"
	}
}
